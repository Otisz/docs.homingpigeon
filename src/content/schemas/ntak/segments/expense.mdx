import {Callout} from "nextra/components";

# Expense schema
Expenditure representing the value of a service consumed.

## Used in
- [Daily closing - `otherExpenses`](/schemas/ntak/daily-closing#otherexpenses)
- [Daily closing - `postStayExpenses`](/schemas/ntak/daily-closing#poststayexpenses)
- [Checkout-day transaction](/schemas/ntak/segments/checkout-day-transaction#expenses)

## Properties
### `amount`
- Type: `number`
- Rules: `required`

<Callout type="info">The amount must be converted to Hungarian Forint.</Callout>

### `payment`
- Type: `enum`
- Rules: `required` | [`oneOf: Payment (enum)`](/schemas/ntak/enums/payment)

### `paymentSubtype`
- Type: `enum` | `null` | `undefined`
- Rules: [`oneOf: Payment subtype (enum)`](/schemas/ntak/enums/payment-subtype)

<Callout type="info">This value is only required when `payment` field value is `szép`.</Callout>

## Object

```ts
export type Expense = {
  amount: number;
  payment: Payment;
  paymentSubtype?: PaymentSubtype | null;
};
```